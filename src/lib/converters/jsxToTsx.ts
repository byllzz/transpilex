export function jsxToTsx(code: string): string {
  // JSX is valid TSX — just add basic type annotations scaffold
  // We don't auto-add types since that requires understanding the code
  return code;
}
